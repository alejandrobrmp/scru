name: PR Validation

on:
  pull_request:

jobs:
  unit-tests:
    uses: ./.github/workflows/unit-tests.yml

  integration-tests:
    needs: unit-tests
    uses: ./.github/workflows/integration-tests.yml
    secrets: inherit
