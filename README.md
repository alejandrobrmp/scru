# SCRU

Simple Cloudflare Record Updater.

## Usage

- `scru` runs the configured updates
- `scru config` opens the config wizard

## Notes

- Linux only
- YAML config in the per-user config directory
- Cloudflare API token is read from an environment variable

This README is intentionally basic and will be updated on each release.
